name: ${project.name}
main: ${project.groupId}.${project.artifactId}.${project.name}Plugin
version: ${project.version}
authors: [jasaush, Ben Bancroft]
softdepends: [BarAPI]

commands:
   game:
      description: Main Plugin Command.
      usage: /game
